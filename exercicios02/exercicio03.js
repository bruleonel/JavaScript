/* Desenvolva uma função que receba o salário/hora
mais a quantidade de horas trabalhas no mês e calcule o
salário total */

function calcularSalario(salario, horas) {
    return salario * horas
}

console.log(calcularSalario(40, 150))